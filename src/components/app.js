import React, { Component, PropTypes } from 'react';

class App extends Component {
  render() {
    return <div>Test</div>;
  }
}

App.propTypes = {
  teams: PropTypes.array,
};

export default App;
